name: Release-Build

on: workflow_dispatch

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  build:
    uses: axonivy-market/github-workflows/.github/workflows/release.yml@v6
    with:
      maven-args: -Divy.engine.version=12.0.10
